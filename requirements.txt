# Backend API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-multipart>=0.0.9
# Core: transcript extraction
youtube-transcript-api>=0.6.2
yt-dlp>=2024.8.6
# AI summarization & audio transcription — Google Gemini only
# (cloud summaries/refined reports, plus transcription fallback)
google-genai>=0.3.0
# Optional: fully offline local audio transcription (open-source Whisper;
# heavy — needs ffmpeg and torch). Not required; Gemini is used without it.
# openai-whisper>=20231117
